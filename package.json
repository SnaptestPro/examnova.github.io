{
  "name": "savyasachi-push-functions",
  "description": "Cloud Function: sends a push notification (via FCM) to every registered device when a test is published.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1"
  },
  "private": true
}
